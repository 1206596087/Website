"use client";
import { useEffect, useState } from "react";
type Item={id:number;kind:string;hanzi:string;romanization:string;title:string;author:string;era:string;body:string;translation:string;meaning:string;tags:string};
export default function Library(){const[q,setQ]=useState("");const[k,setK]=useState("");const[items,setItems]=useState<Item[]>([]);const[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);const r=await fetch(`/api/library?q=${encodeURIComponent(q)}&kind=${k}`);const d=await r.json();setItems(d.items??[]);setLoading(false)};useEffect(()=>{load()},[]);return <main className="library-page"><nav className="nav"><a className="brand" href="/">MÍNGZI<span>{"\u540d"}</span></a><a className="library-admin" href="/admin">Content studio ↗</a></nav><header className="library-head"><p className="eyebrow">The growing collection</p><h1>Search the<br/><em>name library.</em></h1><p>Chinese surnames, given names, classical lines, and public-domain poetry—kept in one living collection.</p></header><section className="library-controls"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} placeholder="Search names, poets, themes, or text"/><select value={k} onChange={e=>setK(e.target.value)}><option value="">All entries</option><option value="surname">Surnames</option><option value="name">Given names</option><option value="poem_zh">Chinese classical poetry</option><option value="poem_en">Public-domain English poetry</option></select><button onClick={load}>Search</button></section><section className="library-results">{loading?<p>Opening the collection…</p>:items.map(i=><article key={i.id} className="library-card"><span>{i.kind.replace("_"," ")}</span><h2>{i.hanzi || i.title}</h2>{i.romanization&&<b>{i.romanization}</b>}{i.author&&<p className="byline">{i.author}{i.era?` · ${i.era}`:""}</p>}<p>{i.body||i.meaning}</p>{i.translation&&<cite>{i.translation}</cite>}{i.tags&&<small>{i.tags}</small>}</article>)}</section></main>}
