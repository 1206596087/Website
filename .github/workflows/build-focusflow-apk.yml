name: Build FocusFlow APK

on:
  push:
    paths:
      - 'focusflow-app/**'
      - '.github/workflows/build-focusflow-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: focusflow-app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'
      - run: gradle assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: focusflow-debug-apk
          path: focusflow-app/app/build/outputs/apk/debug/app-debug.apk
